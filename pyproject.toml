[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "jobmatch_nb"
version = "0.1.0"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
include = ["jobmatch_nb*"]
